node_modules/
data/
*.env
megalinter-reports/
# Local-only scratchpad for pending-redeem notes; never commit.
FEEDBACK

# Replit-specific files
.replit
replit.nix
replit.md
run.sh
scripts/
.local/
attached_assets/

# macOS screenshots dropped into assets/ for editing — keep originals local
# only; the blurred panel-*.png exports are what gets committed.
assets/Screenshot*.png
.local-drafts/
